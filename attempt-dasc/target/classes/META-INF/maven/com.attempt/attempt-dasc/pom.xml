<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.attempt</groupId>
    <artifactId>attempt-core</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    
  </parent>
  <artifactId>attempt-dasc</artifactId>
  <packaging>jar</packaging>
  <description>分布式异步服务调用协议组件</description>
  <dependencies>
        <!--公共组件依赖-->
        <dependency>
            <groupId>com.attempt</groupId>
            <artifactId>attempt-common</artifactId>
            <version>${master_version}</version>
        </dependency>
         <!--消息队列-->
        <dependency>
            <groupId>com.attempt</groupId>
            <artifactId>attempt-queue</artifactId>
            <version>${master_version}</version>
        </dependency>
        <dependency>
            <groupId>com.attempt</groupId>
            <artifactId>attempt-dao</artifactId>
            <version>${master_version}</version>
        </dependency>
   </dependencies>
      <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.19.1</version>
                <configuration>
                    <argLine>-Dfile.encoding=${encoding_type}</argLine>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>2.7</version>
                <configuration>
                    <encoding>${encoding_type}</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.3</version>
                <configuration>
                    <source>${java_version}</source>
                    <target>${java_version}</target>
                    <encoding>${encoding_type}</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.0.0</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>